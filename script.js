const messages=document.querySelector('#messages');const input=document.querySelector('#input');const form=document.querySelector('#composer');const replies=['Absolutely! I can help with that. 🚀','Nice question. In a real app, this message would be sent to your AI backend.','Got it. This demo is ready to connect to an API whenever you want.','That sounds interesting! Tell me a little more and we can build it.'];
function addMessage(text,type){const row=document.createElement('div');row.className=`message ${type}`;row.innerHTML=`<div class="avatar">${type==='assistant'?'✦':'You'}</div><div><div class="bubble"></div><span class="time">Just now</span></div>`;row.querySelector('.bubble').textContent=text;messages.appendChild(row);messages.scrollTop=messages.scrollHeight}
form.addEventListener('submit',e=>{e.preventDefault();const text=input.value.trim();if(!text)return;addMessage(text,'user');input.value='';input.style.height='auto';setTimeout(()=>addMessage(replies[Math.floor(Math.random()*replies.length)],'assistant'),500)});
input.addEventListener('input',()=>{input.style.height='auto';input.style.height=Math.min(input.scrollHeight,120)+'px'});input.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();form.requestSubmit()}});document.querySelector('#clear').onclick=()=>{messages.innerHTML=''};document.querySelector('#newChat').onclick=()=>{messages.innerHTML='';addMessage('New chat started. How can I help?','assistant');input.focus()};